% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/grafico_temperatura_mensual.R
\name{grafico_temperatura_mensual}
\alias{grafico_temperatura_mensual}
\title{Grafico mensual de temperatura por estacion}
\usage{
grafico_temperatura_mensual(datos, colores = NULL, titulo = "Temperatura")
}
\arguments{
\item{datos}{Un data frame con al menos las columnas:
\code{id} (estacion), \code{fecha} (Date o convertible) y
\code{temperatura_abrigo_150cm} (numerica).}

\item{colores}{Vector opcional de colores a usar por estacion. Si su longitud
es menor que la cantidad de estaciones, se recicla.}

\item{titulo}{Titulo del grafico.}
}
\value{
Un objeto \code{ggplot2::ggplot}.
}
\description{
Agrega los datos por mes (1-12) a partir de la columna \code{fecha} y grafica
la temperatura promedio mensual por \code{id} (estacion). Si se pasa un vector
de \code{colores}, se usa; si no, se generan colores automaticamente. Nota:
el calculo mensual agrega todas las observaciones de todos los anos juntos.
}
\examples{
\dontrun{
# df tiene columnas: id, fecha, temperatura_abrigo_150cm
g <- grafico_temperatura_mensual(df, titulo = "Temp promedio por mes")
print(g)
}

}
